import React from 'react'
import './ContainerElastic.css';

const ContainerElastic = ({ children }) => (
  <div className="container-elastic">
    {children}
  </div>
);

export default ContainerElastic;